# member-question-answering
This version uses **TF-IDF (Term Frequency-Inverse Document Frequency)** and **Cosine Similarity** to calculate the relevance of messages to the user's question. This is a robust baseline for semantic search that avoids the C++ compilation issues of libraries like spaCy.
